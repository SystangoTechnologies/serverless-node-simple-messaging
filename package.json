{
  "name": "aws-serverless-messaging",
  "version": "1.0.0",
  "description": "AWS lambda sample for sending emails using AWS SES",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://gitlab.com/systango/open-source/aws-serverless-messaging.git"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.814.0"
  }
}
